namespace JisSDKAds.Ads
{
    /// <summary>Interstitial / Rewarded inventory layout (local default or overridden by Firebase Remote Config).</summary>
    public enum AdInventorySetupMode
    {
        SingleUnit = 0,
        Tiered = 1
    }
}
